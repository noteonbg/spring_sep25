<configuration>

    <!-- Root logger configuration -->
    <root level="INFO">
        <appender-ref ref="console" />
        <!--<appender-ref ref="file" />-->
    </root>

    <!-- Console appender configuration for logging output to the console -->
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="file" class="ch.qos.logback.core.FileAppender">
        <!-- Path to the log file where logs will be written -->
        <file>logs/application.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} - %msg%n</pattern>
        </encoder>
        <!-- Rolling policy to create a new log file daily (optional) -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/application-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory> <!-- Keep logs for the last 30 days -->
        </rollingPolicy>
    </appender>

    <!-- Logging configuration for Spring JDBC, SQL queries will be logged at DEBUG level -->
    <logger name="org.springframework.jdbc.core.JdbcTemplate" level="DEBUG"/>
    <logger name="org.springframework.jdbc.core" level="DEBUG"/>
    <logger name="org.springframework.dao" level="ERROR"/>

</configuration>
